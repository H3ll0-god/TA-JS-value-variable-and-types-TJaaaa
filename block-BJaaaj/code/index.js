
// Accessing and assigning.

let firstName = "Arya";
let lastName = "Stark";

// Do the following:

alert(Arya);// Alert the value of the firstName.
alert(Stark);// Alert the value of the lastName.
firstName = "John"// Assign a new value of `"John"` to firstName.
lastName = "Snow"// Assign a new value of `"Snow"` to lastName.
alert(firstName + " " + lastName);// Alert the new value of `firstName` and `lastName` by concatenating them
const fullName = ${firstName} ${lastName};// Create a new varaible using `const` named `fullName` and assign the fullname using firstName and lastName seperated by space.

// Declare three variables `name,` `newUser` and `isAdmin`.
let name = "Rohan"// Assign your name as the value of `name`.
let newUser = Nivedita// Assign the value of `name` to `newUser`.
isAdmin = `true`// Set the value of `isAdmin` to `true`.
alert(`Rohan` `var1` `isAdmin`);// Alert the values of all the three variables.

// Assignment and re-assignment

var wiseMan = "Tyrion Lannister";

// Follow instructions and do the following. In case of an error write the error in comment

wiseMan = "Samwell Tarly"// Reassign the value of `wiseMan` to "Samwell Tarly"
// Declare a variable `userName` with value "Lysa Arryn"
// Declare a variable named `oddNumber` and assign a value `57`.
// Reassign the value of `oddNumber` to 61

// Mathematical Operation
let userName = "Lysa Arryn"
let amount = 4280;
// Declare a variable named `oddNumber` and assign a value `57`.
let oddNumber = 57;
// Reassign the value of oddNumber to 61
oddNumber = 61;  // oddNumber is now 61

// Using mathematical operations find the solutions. `(+, -, \*, / , etc.)`

let amount = 4280;

// Using mathematical operations find the solutions. `(+, -, \*, / , etc.)`


// Declare a new variable `reducedAmount`. In it store the value that is 24 less than the value of amount.
let reducedAmount = amount - 24;
// Declare another variable `addedAmount`. It's value should be 32 more than the value of amount.
let addedAmount = amount + 32;
// Declare a variable `multipleAmount`. It's value should be 7 times the value of amount.
let multipleAmount = amount *7;
// Declare a variable `dividedAmount`. It should store the resultant of amount divided by 57.

let dividedAmount = amount / 57;
